<nav class="navbar navbar-light bg-light">
  <a class="nav-link">Crew Leaders VBS Colombia</a>
  <%= form_tag crew_leaders_path, method: :get, class: "form-inline" do %>
      <%= text_field_tag :first_name, params[:first_name], class: "form-control mr-sm-2", placeholder: "Nombre" %>
      <br><br>
      <%= text_field_tag :church, params[:church], class: "form-control mr-sm-2", placeholder: "Iglesia" %>
    <%= submit_tag "Buscar", name: nil, class: "btn btn-outline-info my-2 my-sm-0" %>
  <% end %>
</nav>

<div class="table-responsive-sm">
  <table class="table">
    <thead class="thead-light">
    <tr>
      <th>Nombre(s)</th>
      <th>Apellidos(s)</th>
      <th>Email</th>
      <th>Teléfono</th>
      <th>Iglesia</th>
      <th>Editar</th>
      <th>Eliminar</th>
    </tr>
    </thead>
    <br>
    <tbody>
      <% @crew_leaders.each do |crew_leader| %>
        <tr>
          <td><%= link_to crew_leader.first_name, crew_leader_path(crew_leader) %></td>
          <td><%= crew_leader.last_name %></td>
          <td><%= crew_leader.email %></td>
          <td><%= crew_leader.phone %></td>
          <td><%= crew_leader.church %></td>
          <td>
            <%= link_to '<i class="fas fa-edit"></i>'.html_safe, edit_crew_leader_path(crew_leader), class: "btn btn-info" %>
          </td>
          <td>
            <%= link_to '<i class="far fa-trash-alt"></i>'.html_safe, { action: :destroy, id: crew_leader.id }, method: :delete, data: { confirm: 'Estás seguro?' }, class: "btn btn-danger" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
